'use strict';

describe('OSFitter module', function() {

  beforeEach(module('OSFitter'));

  describe('outfitter controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('OutfitterViewController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});